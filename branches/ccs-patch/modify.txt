----------
Regarding linux-2.6.*/security/Kconfig , insert a line

source security/ccsecurity/Kconfig

before the last

endmenu

line.
----------
Regarding linux-2.6.*/security/Makefile , append the below two lines.

subdir-$(CONFIG_CCSECURITY) += ccsecurity
obj-$(CONFIG_CCSECURITY) += ccsecurity/built-in.o

----------
Regarding linux-2.4.*/fs/Config.in , insert a line

source security/ccsecurity/Config.in

before the last

endmenu

line.
----------
Regarding linux-2.4.*/fs/Makefile , insert a line

subdir-$(CONFIG_CCSECURITY)     += ccsecurity

before the

# persistent filesystems

line.
----------
About linux-2.4.* , make a symlink to ../security/ccsecurity from fs/ .
